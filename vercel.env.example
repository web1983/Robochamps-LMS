# Copy each name + value into Vercel → Settings → Environment Variables
# Enable for: Production (and Preview if you use preview URLs)
# Then: Deployments → Redeploy (turn OFF "Use existing Build Cache")

MONGO_URI=
JWT_SECRET=
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
NODE_ENV=production
FRONTEND_URL=https://robochamps-lms.vercel.app
