{
  "name": "robochamps-lms",
  "private": true,
  "scripts": {
    "build:client": "npm run build --prefix client",
    "install:all": "npm install --prefix server && npm install --prefix client && npm install --prefix api"
  }
}
