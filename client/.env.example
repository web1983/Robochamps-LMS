# Local dev: leave unset — Vite proxies /api to localhost:8080 (see vite.config.js)
# Vercel frontend: set in Project → Settings → Environment Variables (Production)
# VITE_API_URL=https://your-backend-project.vercel.app
