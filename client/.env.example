# Local dev: leave VITE_API_URL unset (Vite proxies /api → http://localhost:8080)
#
# Production (optional — same domain as frontend):
# VITE_API_URL=https://robochamps-lms.vercel.app
#
# Do NOT use http://localhost:8080/api/v1 here — that doubles /api/v1 in URLs.
